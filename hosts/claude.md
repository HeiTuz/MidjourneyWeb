# Claude Code host

Use the browser tool currently exposed to this Claude Code session, preferring the user's chosen authenticated browser. Codex's in-app browser and `cua_repl` names in the shared references are host-specific examples, not callable Claude Code tools. Inspect the available browser tool documentation and use its supported snapshots, clicks, uploads, downloads and waits.

Installation supplies the shared Midjourney workflows, not a browser MCP server or a login session. If browser control is absent, explain that concrete limitation and complete any requested prompt preparation without claiming site execution. Do not install a server or copy credentials without user authorization. Browser execution on this host is not yet live-tested by this project.
