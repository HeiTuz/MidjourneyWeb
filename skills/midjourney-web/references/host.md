# Source-checkout host

For direct source use in Codex, follow the currently supplied in-app browser tools. The installer replaces this file with the selected host's adapter. Read that adapter before using the shared browser workflow; a Codex API example is not an instruction to invent the same API in another host.
